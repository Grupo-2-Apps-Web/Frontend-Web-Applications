export class Vehicle {
    constructor(id, model, plate, tractor_model, max_load, volume){
        this.id = id;
        this.model = model;
        this.plate = plate;
        this.tractor_model = tractor_model;
        this.max_load = max_load;
        this.volume = volume;
    }
}